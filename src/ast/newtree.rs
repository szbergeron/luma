use crate::helper::interner::IStr;

/*pub struct Node {
    name: IStr,
    id: NodeID,

    inner: NodeInner,

    direct_children: Vec<NodeID>,

    parent: NodeID, // set to self.id if root node
}*/
